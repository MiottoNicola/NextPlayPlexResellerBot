name: Deploy NextPlayPlexResellerBot

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 212.227.22.123
          username: nicola
          port: 2222
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/telegram.miottonicola.it/NextPlayPlexResellerBot
            git fetch origin main
            git reset --hard origin/main
            git clean -fd
